标准数据类型：整型、布尔型、浮点型、复数型、字符串、列表、元组、字典

1、python中整型基本上不会有溢出的error
2、布尔型是整型的子类（True, False），本质上是用整型的1,0分别存储的
3、浮点型即数学中的实数，可以类似科学计数法表示 9.8e3
4、复数=实数+虚数，而虚数j=sqrt(-1)且虚数部分必须有j，如：
   x=2.4+5.6j
   x.imag   #分离复数
   x.real   #分离实数
   x.conjugate   #获得共轭复数
5、序列类型：
   列表，可变类型
   元组，不可变类型
   字符串，不可变类型，可用单引号，双引号，三引号(三个单引号)表示
          myString='Hello World!'
          myString[1] or +运算
6、映射类型：字典{}，键值对
            d={'sine:'sin','cosine:'cos','PI':3.14159}
            d['sine']   #由键获取值
